import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.almirzulic.agnitio',
  appName: 'agnitio',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
